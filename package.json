{
  "name": "env-cli",
  "version": "0.1.0",
  "description": "A CLI for copy and paste your local .env to right projects faster",
  "module": "./src/index.ts",
  "type": "module",
  "main": "./dist/index.js",
  "exports": "./dist/index.js",
  "bin": {
    "env-cli": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
    "bun.lockb",
    "package.json"
  ],
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "build": "bun build.ts",
    "test": "bun test",
    "format": "biome check --apply . && biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.2",
    "@types/inquirer": "^9.0.7",
    "@types/minimist": "^1.2.5",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "inquirer": "^9.2.12",
    "minimist": "^1.2.8"
  }
}
